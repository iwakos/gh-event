name: deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: make and upload
        run: |
          pwd
          echo " make test.txt"
          touch test.txt
          echo "aa" > test.txt
        uses: actions/upload-artifact@v3
        with:
          name: txt-files
          path: ./